import {Snackbar as RNPSnackbar, MD3Colors} from 'react-native-paper';
import styled from 'styled-components/native';

export const Snackbar = styled(RNPSnackbar)`
  background-color: ${MD3Colors.error40};
`;
